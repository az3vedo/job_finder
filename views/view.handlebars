<div id="job-view-container" class="container">
  <div class="row text-center">
    <a href="/" class="back-link main-color">Voltar</a>
    <div id="img-container" class="col-md-12">
      <img src="/img/company.svg" alt="Company">
    </div>
    <div class="col-md-12">
      <h1 class="main-color">{{dataValues.title}}</h1>
      <p class="bold">Quem eles estão buscando</p>
      <p class="job-desc">{{dataValues.description}}</p>
      <p><span class="bold">O salário informado é de: </span>R${{dataValues.salary}}</p>
      <p>
        Para trabalhar na <span class="bold main-color">{{dataValues.company}} </span>envie um e-mail para a <a href="mail:to{{dataValues.email}}">{{dataValues.email}}</a>
      </p>
    </div>
  </div> 
</div>